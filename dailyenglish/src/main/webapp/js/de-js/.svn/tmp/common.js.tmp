var wrConst = {
		Status_UserIsNotLogin : 0,
		Status_UserIsLogin : 1,
		
		UserRole_Administrator : 1,
		UserRole_SystemAdministrator : 2,
		UserRole_Member : 3
};

var pageState = new Object();
pageState.sort = {
	    key:"title",
	    dir:1
};

function formatPostDate(date){
	var dateStr = "";
	if(date.getYear() > 100){
		dateStr += "20" + (date.getYear())%100;
	}else{
		dataStr += "19" + (date.getYear())%100;
	}
	dateStr += "/" + String(date.getMonth() + 1);
	dateStr += "/" + date.getDate();
	var hour = date.getHours();
	var minute = date.getMinutes();
	if(hour == 0){
		dateStr += " 00";
	}else if(hour>0 && hour<10){
		dateStr += " 0" + hour;
	}
	if(minute == 0){
		dateStr += ":00";
	}else if(minute>0 && minute<10){
		dateStr += ":0" + minute;
	}
	return dateStr;
}

function clickTable(text){
	var id = text.attr('id');
	if(id == 'htitle' || id == 'hauthor' || id == 'htime'){
		if(id == 'htitle'){
			id = id.substr(1, id.length-1);
		}else if(id == 'hauthor'){
			id = "userid";
		}else if(id == 'htime'){
			id = "createtime";
		}
		sort(id);
	}else if(id == 'title'){
		showContent(text);
	}else if(id == 'author'){
		showAuthor();
	}
}

function sort(id){
/*	pageState.sort.key = id;
	pageState.sort.dir = 1;*/
	if(pageState.sort.key == id){
		pageState.sort.dir = Number(!pageState.sort.dir)
	}else{
		pageState.sort.key = id;
		pageState.sort.dir = 1;
	}
	makeTable(); 
	$('table#demo').bestTable();
}

function showContent(text){
	alert("this is title");
}

function showAuthor(){
	alert("this is author");
}

function showToolTip(obj){
	//alert("hahah");
	//alert($(obj).attr("offsetTop"));
	createDiv();  
	var tipTitle = "click to sort";
	$("#tooltip").show();
	$("#tooltip").html = "闻欢";
}

function createDiv(){
	var divTips = document.createElement("div"); //create div element
	divTips.id='tooltip';
	divTips.style.display='none';    //invisible
	document.body.appendChild(divTips); 
}

function highlightLeftNav(li){
	document.getElementById(li).className = "active";
}